%h1 Edit Review for #{@movie.title}

= form_tag movie_review_path(@movie), :method => :put do
  = render :partial => 'review_form'
  %div
  = submit_tag 'Save Changes'
  %div
  = link_to 'Cancel', movie_path(@movie)